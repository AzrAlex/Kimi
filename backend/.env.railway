# Railway Environment Configuration
# These variables will be set via Railway Dashboard
# This file is for reference only

# MongoDB Configuration (set via Railway MongoDB service)
# MONGO_URL=${{MongoDB.MONGO_URL}}
# DB_NAME=stockify

# Python Configuration
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1

# JWT Configuration (customize as needed)
SECRET_KEY=stockify-railway-secret-key-2025
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Railway will provide PORT automatically
# PORT=$PORT